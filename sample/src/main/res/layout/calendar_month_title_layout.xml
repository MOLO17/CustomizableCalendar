<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <android.support.v7.widget.AppCompatImageView
        android:id="@+id/prevIcon"
        app:srcCompat="@drawable/ic_chevron_left"
        app:tint="#f3484e"
        app:layout_constraintTop_toTopOf="@android:id/message"
        app:layout_constraintBottom_toBottomOf="@android:id/message"
        app:layout_constraintRight_toLeftOf="@android:id/message"
        android:layout_marginRight="16dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>


    <android.support.v7.widget.AppCompatImageView
        android:id="@+id/nextIcon"
        app:srcCompat="@drawable/ic_chevron_right"
        app:tint="#f3484e"
        app:layout_constraintTop_toTopOf="@android:id/message"
        app:layout_constraintBottom_toBottomOf="@android:id/message"
        app:layout_constraintLeft_toRightOf="@android:id/message"
        android:layout_marginLeft="16dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@android:id/message"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:gravity="center_vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        tools:text="СЕНТЯБРЬ 2018"
        android:textAllCaps="true"
        android:textColor="#f3484e"
        android:textSize="18sp"/>

</android.support.constraint.ConstraintLayout>